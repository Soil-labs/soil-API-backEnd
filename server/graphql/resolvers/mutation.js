const {add_member_skill} = require("./mutation/memberMutation");
const {updateProject} = require("./mutation/projectsMutation");



module.exports = {
  Mutation: {
    // ------------- USER MUTATION -----------------

    add_member_skill,
    updateProject,

    // ------------- USER MUTATION -----------------

  
    
  },
};