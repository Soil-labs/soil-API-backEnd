module.exports = `
    
    type Skills {
        _id: ID
        tagName: String,

        tweets: [String],
        members: [Members],
        authors: [Members],

        registeredAt: String,
        approvedSkill: approvedSkillEnum,
    }

    input findSkillsInput {
        tagName: [String]
    }

    input findSkillInput {
        tagName: String
    }

    input createSkillInput {
        tagName: String
    }

    enum approvedSkillEnum {
        waiting
        rejected
        approved
    }
    

`